core:
  raise_on_error: false

inventory:
  plugin: FileInventoryPlugin
  options:
    hosts_file: ./inventory/hosts.yaml

runner:
  plugin: threaded
  worker_count: 10
  retry:
    allow: true
    max_attempts: 4
    delay_ms: 250

logging:
  level: info
  max_file_count: 10
