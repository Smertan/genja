[package]
name = "genja"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1.89"
genja-core = { path = "../genja-core" }
genja-plugin-manager = { path = "../genja-plugin-manager" }
log = "0.4.27"
regex = "1.12.2"
serde_json = "1.0.142"
tokio = { version = "1.48.0", features = ["macros", "rt-multi-thread", "sync"] }

[dev-dependencies]
genja-core-derive = { path = "../genja-core-derive" }
