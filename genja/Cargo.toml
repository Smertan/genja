[package]
name = "genja"
version = "0.1.0"
edition = "2024"

[dependencies]
genja-core = { path = "../genja-core" }
genja-plugin-manager = { path = "../genja-plugin-manager" }

[dev-dependencies]
serde_json = "1.0.142"
